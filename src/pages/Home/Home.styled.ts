import { Box, styled } from '@mui/material'

export const HomeContainer = styled(Box)({
    flex: 1,
    display: 'flex',
    width: '100%',
    height: '100%',
    justifyContent: 'center',
    alignItems: 'center',
})
