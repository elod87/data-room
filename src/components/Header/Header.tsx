import { Typography } from '@mui/material'

import { HeaderContainer } from './Header.styled'

const Header = () => {
    return (
        <HeaderContainer>
            <Typography variant="h6">Acme Corp - Data Room</Typography>
        </HeaderContainer>
    )
}

export default Header
